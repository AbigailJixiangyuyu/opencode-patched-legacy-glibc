#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
LIB_DIR="$(cd "$SCRIPT_DIR/../lib" && pwd)"

export LD_PRELOAD="$LIB_DIR/clear_ldpath.so"

exec "$LIB_DIR/opencode" "$@"